专业技能：

- 前端基础：熟悉HTML，CSS，JavaScript（ES6+）和TypeScript，理解闭包、原型链、事件循环等核心概念，具备异步编程经验（Promise / async-await）。
- 框架能力：熟悉Vue2/3框架，了解Composition API、响应式、组件通信、路由、状态管理等，具备 Vue3 + Element Plus项目开发经验。
- 工程化：了解前端工程化，使用pnpm/yarn进行包管理，了解Vite等打包工具及其配置过程，掌握Git的基本使用。具备使用 AI 工具辅助开发的能力。
- 了解Node.js基本使用，可进行简单服务搭建、接口联调与开发调试
- 了解计算机网络核心知识（HTTP协议、TCP/IP协议等），了解浏览器知识（缓存/渲染等）。

重点准备  **Event Loop、Promise、HTTP缓存、Vue响应式原理、手写深拷贝/防抖节流** 。

---

## 一、前端基础

### JavaScript 核心机制

* 解释闭包的本质？它解决什么问题？常见误用有哪些？
* 举一个闭包导致内存泄漏的场景，如何排查？
* 原型链查找过程是怎样的？`__proto__` 和 `prototype` 的区别？
* `new` 操作符做了哪些事情？
* `this` 绑定规则有哪些？箭头函数为什么没有 `this`？
* 手写实现：

  * `call / apply / bind`
  * 简化版 `new`
* 深拷贝怎么实现？`JSON.parse(JSON.stringify())` 有什么问题？

### 事件循环（重点）

* 解释 Event Loop 的完整流程（宏任务 / 微任务）
* `Promise.then`、`setTimeout`、`MutationObserver` 执行顺序
* Node.js 和浏览器 Event Loop 的区别
* 代码题（必须能推执行顺序）

### 异步编程

* Promise 状态流转？为什么是不可逆的？
* `async/await` 本质是什么？是语法糖吗？
* 如何实现并发控制（比如限制同时最多 5 个请求）？
* 如何处理请求取消？（AbortController / axios cancel）

---

## 二、Vue 能力

### Vue2 vs Vue3

* Vue2 和 Vue3 响应式原理的区别（Object.defineProperty vs Proxy）
* 为什么 Vue3 更快？具体体现在哪？
* Composition API 相比 Options API 的优势和代价？

### 响应式系统

* `ref` vs `reactive` 区别？使用场景？
* `computed` 和 `watch` 的区别？底层实现思路？
* `watchEffect` 和 `watch` 区别？

### 组件设计

* 父子组件通信有哪些方式？适用场景？
* 跨组件通信怎么做？（provide/inject、pinia、event bus）
* 如何设计一个高复用组件？考虑哪些点？

### 生命周期

* Vue3 生命周期有哪些？和 Vue2 对比
* `setup` 执行时机？
* DOM 可访问是在什么时候？

### 路由 & 状态管理

* Vue Router 的实现原理（hash vs history）
* 路由守卫执行顺序
* Pinia vs Vuex 的区别
* 状态持久化怎么做？

### 实战问题（很关键）

* 项目中遇到最复杂的问题是什么？怎么解决？
* 页面性能优化做过哪些？
* 如何做组件拆分？有没有设计规范？
* Element Plus 是否做过二次封装？

---

## 三、工程化

### 构建工具（Vite）

* Vite 为什么比 Webpack 快？
* Vite 的核心机制（依赖预构建 + ESM）
* Vite 和 Webpack 的本质区别
* 如何配置别名、环境变量？
* 打包优化做过哪些？

### 包管理

* pnpm 和 npm/yarn 的区别？
* pnpm 为什么更省磁盘？
* lockfile 的作用？

### Git

* 常用命令（rebase vs merge）
* 如何解决冲突？
* Git flow 或团队协作流程？

### AI 工具

* 用过哪些 AI 工具？具体解决了什么问题？
* 是否用 AI 生成过代码？如何保证质量？
* AI 是否参与过代码重构或测试？

---

## 四、Node.js

* Node.js 事件循环和浏览器有什么区别？
* 如何搭建一个简单服务（Express / Koa）？
* 中间件机制是什么？
* 如何处理跨域？
* 是否写过接口？如何做参数校验？
* 如何做日志、错误处理？

---

## 五、计算机网络

### HTTP

* HTTP1.1 vs HTTP2 vs HTTP3 区别
* 常见状态码（不仅是 200/404）
* GET vs POST 的本质区别
* 如何做缓存（强缓存 vs 协商缓存）
* Cookie / Session / Token 区别

### TCP/IP

* TCP 三次握手 / 四次挥手
* 为什么是三次？
* TCP 和 UDP 区别

---

## 六、浏览器原理

* 浏览器从输入 URL 到页面渲染的完整过程
* 渲染流程（DOM、CSSOM、Render Tree）
* 重排（reflow）和重绘（repaint）
* 如何优化性能（减少重排）
* 浏览器缓存机制

---

## 七、综合能力

### 性能优化

* 首屏优化怎么做？
* 懒加载 / 预加载区别
* 如何分析性能（Lighthouse / Performance）

### 安全

* XSS、CSRF 原理及防御
* 如何做输入过滤？

### 代码设计

* 如何保证代码可维护性？
* 是否做过重构？
* 如何设计目录结构？

---

## 八、压力测试类问题

* 如果页面白屏，你怎么排查？
* 接口很慢，你怎么优化？
* 用户反馈卡顿，你如何定位？
* 一个复杂表单如何设计（性能 + 可维护）？

## 面试重点策略（作为面试官）

* 每个知识点至少追问 2 层（原理 → 场景 → 代码）
* 多问“为什么”，少问“是什么”
* 强制结合实际项目，不接受纯概念回答
* 给现场题（Event Loop / Vue 响应式 / 手写）

---
